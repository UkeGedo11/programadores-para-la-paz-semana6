Explicacion.

Al enviar una peticion POST a la ruta /registro con un JSON que contiene los campos nombre y mensaje, 
el servidor recibe la informacion mediante req.body. Luego responde con un objeto JSON que confirma 
que los datos fueron recibidos correctamente, mostrando el estado, el nombre y el mensaje enviados por el usuario.